/* Visual Studio Code: HotReloading/Program.cs using dotnet watch
- dotnet watch run

 change the output message while running the code to see the changes without manually rebuilding the app again */

while (true)
{
    Console.WriteLine("Goodbye, watch!");
    Thread.Sleep(2000);  // delay for 2 seconds
}